# MEMO — 项目记忆与进度

## 需求（用户原话要点）

* 开放世界 roguelike，世界历史演化（国家 / 民族 / 魔法 / 科技 / 矿产 / 农业 / 天气）由大模型决定。
* **树状生成粒度分层**；地图有不同 level of detail 以降低大模型上下文需求。
* **无图形界面**，只显示主角周围 16x16 格子；"点击"格子显示说明文本。
* 每格内物体（土地、岩石、树木…）描述由大模型生成并**存入 Python 数据库**；
  下一时刻的演化也由大模型决定，**演化后的对象状态都要落库**。
* 地图随玩家探索**逐步生成**。
* 演化时既检索数据库中位置相邻的对象，也综合**更高 LOD 的历史演化结果**，再由大模型生成下一时间步。
* 上下文上限 140000，需**限制**世界大小、国家数量、NPC 数量。
* 故事线与 NPC 对话由 LLM 实时生成；战斗与基本移动由 Python 逻辑完成，并及时更新 LLM 上下文。
* 可用 `127.0.0.1:8080` 的模型测试。

## 当前进度（已完成）

* [x] 项目骨架、配置、`.gitignore`、git 初始化
* [x] `llm.py`：OpenAI 兼容客户端、SQLite 响应缓存、JSON 修复、截断重试、确定性 Mock 后端、自动降级
* [x] `db.py`：12 张表的 SQLite schema 与 DAO
* [x] `world.py`：LOD 树自顶向下按需生成、地块材料化、格子描述按需生成、出生点寻址
* [x] `evolution.py`：四层 LOD 时钟、邻居 + 更高层历史综合、delta 白名单落地、NPC 漂移
* [x] `entities.py`：玩家 / 移动 / 碰撞 / 战斗 / 升级（纯 Python）
* [x] `narrative.py`：故事线导演 + 实时 NPC 对话
* [x] `render.py` + `game.py`：16x16 文本视图、"点击"查看、REPL、自动探索
* [x] 32 个 unittest（全 Mock，零网络）+ 冒烟脚本 + LLM 连通性脚本
* [x] 真实模型联调通过（seed 777，13 次实调，约 13 分钟跑完生成→探索→对话→战斗→演化→任务全流程）
* [x] 真实演化链验证：world/region/zone 事件确实下沉为地块级事件（日志可见"子区域"事件被地块事件承接）

### 真实模型实测数据（127.0.0.1:8080，Qwen3.8-27B-IQ3_XXS）

| 任务 | 输入 tok | 输出 | 耗时 |
| --- | --- | --- | --- |
| world | ~520 | 1300 字 | 45–58 s |
| region | ~880 | 750 字 | 37 s |
| zone | ~1070 | 760 字 | 39 s |
| chunk | ~970 | 830 字 | 39–45 s |
| tile | ~730 | 150 字 | 10 s |
| story | ~1040 | 220 字 | 14 s |
| evolve_* | ~1250–1450 | 580–810 字 | 31–36 s |

## 待办 / 可继续的方向

* [ ] 真实模型下的完整长程联调（多轮演化 + 对话 + 战斗）耗时较长，需要 tmux 慢跑观察
* [ ] 演化时把"更高 LOD 的演化结果"再做一次**按坐标相交**的过滤（目前按祖先链 + 世界事件）
* [ ] `travel` 快速旅行（跨 chunk 跳跃但按比例补算演化）
* [ ] 存档版本迁移（schema 变更时）
* [ ] 可选：把 chunk 地形矩阵改为纯程序化（由 zone 的 LLM `biome_mix` 驱动），省约 40% 生成时间

## 关键决策

* **数据库是唯一事实来源**，LLM 只读摘要、只写 delta。上下文量与地图面积解耦。
* 固定 system prompt 以复用 llama.cpp 前缀缓存。
* 关闭 thinking（`enable_thinking: false`），否则 `max_tokens` 会被推理内容吃光。
* 地形用**单字符代码矩阵**表达（省 token），并带**退化检测 + 程序化回退**。
* 本地模型不可达时自动降级到 Mock 后端，保证游戏始终可玩、测试始终可跑。
* 大型/耗时任务统一用 tmux 跑，日志写 `logs/`，用 `tmux capture-pane` / 读日志查看进度。
