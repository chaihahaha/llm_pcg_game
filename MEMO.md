# MEMO — 项目记忆与进度

## 需求（用户原话要点）

* 开放世界 roguelike，世界历史演化（国家 / 民族 / 魔法 / 科技 / 矿产 / 农业 / 天气）由大模型决定。
* **树状生成粒度分层**；地图有不同 level of detail 以降低大模型上下文需求。
* **无图形界面**，只显示主角周围 16x16 格子；"点击"格子显示说明文本。
* 每格内物体（土地、岩石、树木…）描述由大模型生成并**存入 Python 数据库**；
  下一时刻的演化也由大模型决定，**演化后的对象状态都要落库**。
* 地图随玩家探索**逐步生成**。
* 演化时既检索数据库中位置相邻的对象，也综合**更高 LOD 的历史演化结果**，再由大模型生成下一时间步。
* 上下文上限 140000，需**限制**世界大小、国家数量、NPC 数量。
* 故事线与 NPC 对话由 LLM 实时生成；战斗与基本移动由 Python 逻辑完成，并及时更新 LLM 上下文。
* 可用 `127.0.0.1:8080` 的模型测试。

## 当前进度（已完成）

* [x] 项目骨架、配置、`.gitignore`、git 初始化
* [x] `llm.py`：OpenAI 兼容客户端、SQLite 响应缓存、JSON 修复、截断重试、确定性 Mock 后端、自动降级
* [x] `db.py`：12 张表的 SQLite schema 与 DAO
* [x] `world.py`：LOD 树自顶向下按需生成、地块材料化、格子描述按需生成、出生点寻址
* [x] `evolution.py`：四层 LOD 时钟、邻居 + 更高层历史综合、delta 白名单落地、NPC 漂移
* [x] `entities.py`：玩家 / 移动 / 碰撞 / 战斗 / 升级（纯 Python）
* [x] `narrative.py`：故事线导演 + 实时 NPC 对话
* [x] `render.py` + `game.py`：16x16 文本视图、"点击"查看、REPL、自动探索
* [x] 32 个 unittest（全 Mock，零网络）+ 冒烟脚本 + LLM 连通性脚本
* [x] 真实模型联调通过（seed 777，13 次实调，约 13 分钟跑完生成→探索→对话→战斗→演化→任务全流程）
* [x] 真实演化链验证：world/region/zone 事件确实下沉为地块级事件（日志可见"子区域"事件被地块事件承接）

### 真实模型实测数据（127.0.0.1:8080，Qwen3.8-27B-IQ3_XXS）

| 任务 | 输入 tok | 输出 | 耗时 |
| --- | --- | --- | --- |
| world | ~520 | 1300 字 | 45–58 s |
| region | ~880 | 750 字 | 37 s |
| zone | ~1070 | 760 字 | 39 s |
| chunk | ~970 | 830 字 | 39–45 s |
| tile | ~730 | 150 字 | 10 s |
| story | ~1040 | 220 字 | 14 s |
| evolve_* | ~1250–1450 | 580–810 字 | 31–36 s |

## 长跑一致性测试（2026-09-23）

用户要求：主角走远再回来，检查旧地点是否正常演化、有无不合逻辑；
角色关系图与复杂剧情线是否正常演化，NPC 是否失忆或与历史矛盾。

新增 `scripts/qa_consistency.py`：A 探索 -> 远行 B -> 200 小时流逝 -> 返回 A，
做结构性断言 + `--audit` 让 LLM 通读档案找矛盾。

**真实模型（seed 2026/777，31 次调用，约 25 分钟）发现并修复：**
* 世界只有玩家脚下在演化（远离的区域被冻结）→ 遍历全部已生成节点调度 + 距离分级周期 + 追赶式摘要
* 演化 delta 坐标系不明 → 提示词加 `[[SCOPE:x,y,w,h]]` 并强制世界坐标
* 没有关系图 → 新增 `relations` 表（世界生成播种国家外交、chunk 生成播种 NPC 关系、对话/演化可更新）
* NPC 没有长期记忆 → 新增 `npc_memory` 表 + 超限时 LLM 压缩；对话历史按 NPC 分别保留
* 故事线跨区域失忆 → 提示词加入历次任务 saga 与世界级事件
* 物体被删导致历史丢失 → 软删除（`alive`）+ 描述变更保留旧文本
* **HTTPError 被当作断线从而静默降级 Mock**（用虚构内容冒充模型输出）→ 只对真正的连接失败降级
* LLM 审计发现：[高危] NPC 处境（昏迷等）未被记录 → 新增 `npcs.status`；
  [中危] NPC 重复同一行动 → 每 NPC 带"上一步/记忆/近事" + 单步位移上限；
  [低危] 记忆与事件数字不符、编造外貌 → 对话注入"最近亲历之事" + 新增 `npcs.appearance`
* 追赶演化的事件按比例散布在时间窗内，而不是全部打在同一时刻

### 五轮真实模型长跑的一致性审计结果

评估口径：`scripts/qa_consistency.py`（结构断言）+ `--audit`（LLM 通读档案找矛盾）。
每轮都针对根因修复，而不是压测通过。

| 轮次 | seed | 结构性问题 | LLM 审计 | 报告 |
| --- | --- | --- | --- | --- |
| 1 | 2026 | 2 | 2 高危 + 1 中 + 1 低 | outputs/qa_real.md |
| 2 | 777 | 0 | 2 高危 + 1 中 + 1 低 | outputs/qa_real2.md |
| 3 | 4242 | 1（社交图为空） | 1 中 + 1 低 | outputs/qa_real3.md |
| 4 | 9090 | 0 | 1 高危 + 2 中 + 1 低 | outputs/qa_real4.md |
| 5 | 31337 | **0** | **0 高危** + 1 中 + 1 低 | outputs/qa_real5.md |

各轮暴露并修复的根因（详见 PITFALL.md 17–24）：

1. HTTPError 被当作断线 → 静默降级 Mock（用虚构内容冒充模型输出）
2. 世界只有玩家脚下在演化 → 全节点调度 + 距离分级周期 + 追赶式摘要
3. 演化 delta 坐标系不明 → `[[SCOPE:x,y,w,h]]` + 强制世界坐标
4. 无关系图 / NPC 无长期记忆 → `relations` + `npc_memory` 表，超限时 LLM 压缩
5. 物体被删丢历史 → 软删除 + 残迹 + 原地转化，且残迹仍显示在地图上
6. NPC 状态（昏迷等）不落库 → `npcs.status`
7. 同一 NPC 重复同一行动 / 瞬移 → 每 NPC 带"上一步/记忆/近事" + 单步位移上限
8. 组织撤离后"复活" → 本层往事滚动摘要（`node.data.history`）
9. NPC 身份漂移、编造外貌、凭猜断言阴谋 → 身份字段不可变 + 亲历之事注入 + 明文禁止
10. 已毁物被重复宣告 → `[已毁]` 标记 + 改名改类型为"残迹"

第 5 轮仅剩两条低中危：NPC 旧记忆未随物体移动更新（已加提示词规则）、
关系图里有未在事件流出现的 NPC（属正常，NPC 可以在没有事件的情况下存在）。

## 自由动作与运行时 monkey patch（新增）

用户反馈：能做的动作太少，希望用自然语言描述意图，并允许 LLM 实时改写游戏程序
（不是改文件，而是改游戏里的 Python 对象与 lambda）。

已实现：
* `do <自由文本>`（未识别的输入也默认走这里）→ 模型输出**动作程序**：
  白名单 effects（改地形/增删物体/移动/治疗/给予/事件/记忆/关系）+ 耗时 + 叙事，
  可附带 `new_action`（存成新指令，之后 `do dig` 直接回放、不再花 LLM 调用）
  与 `patch`（改规则/挂 hook/写沙箱代码）
* `rules` / `actions` 命令查看被改写的世界法则、钩子、补丁源码与学会的动作
* 沙箱 `sandbox.py`：表达式层（`eval` 编译 lambda 挂到 hook）与语句层（`exec` 跑补丁），
  两者都 AST 白名单 + 无 builtins + 禁 import/while/dunder + 整数上限；
  坏补丁读档时自动停用；hook 抛错不影响游戏
* 六个真实钩子：`can_enter` / `on_enter` / `damage` / `speech` / `move_cost` / `evolve_bias`
* 世界演化也能产出 `type=patch`（上古诅咒、神谕这类事件会真的改程序行为）
* `patches` / `actions` 两张表持久化，诅咒跨会话生效

真实模型验证（seed 8888，8 次调用，EXIT=0）：
* "向下挖一口通往地底的竖井，用挖出的土在井口垒一圈" →
  地形改 `cave` + 生成"井口土垒"物体 + 学会动作「挖掘竖井」
* "施加诅咒：所有活着的东西说话不能超过十个字" →
  装上 `npc_speech_max_chars=10`，`rules` 可见，`talk` 立刻只输出十个字；
  后续演化事件还承接了它（"静默诅咒生效，生物言语受到物理层面限制"）
* "砍倒东南方的那棵树" → 正确判定不可行（该区域是金属废土，没有树）

## 待办 / 可继续的方向

* [ ] 真实模型下的完整长程联调（多轮演化 + 对话 + 战斗）耗时较长，需要 tmux 慢跑观察
* [ ] 演化时把"更高 LOD 的演化结果"再做一次**按坐标相交**的过滤（目前按祖先链 + 世界事件）
* [ ] `travel` 快速旅行（跨 chunk 跳跃但按比例补算演化）
* [ ] 存档版本迁移（schema 变更时）
* [ ] 可选：把 chunk 地形矩阵改为纯程序化（由 zone 的 LLM `biome_mix` 驱动），省约 40% 生成时间

## 关键决策

* **数据库是唯一事实来源**，LLM 只读摘要、只写 delta。上下文量与地图面积解耦。
* 固定 system prompt 以复用 llama.cpp 前缀缓存。
* 关闭 thinking（`enable_thinking: false`），否则 `max_tokens` 会被推理内容吃光。
* 地形用**单字符代码矩阵**表达（省 token），并带**退化检测 + 程序化回退**。
* 本地模型不可达时自动降级到 Mock 后端，保证游戏始终可玩、测试始终可跑。
* 大型/耗时任务统一用 tmux 跑，日志写 `logs/`，用 `tmux capture-pane` / 读日志查看进度。
